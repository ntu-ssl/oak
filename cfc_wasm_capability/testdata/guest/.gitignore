# Generated by cargo-component / cargo when building the guest fixtures.
# The checked-in artifacts are the .wasm files in ../ (testdata/*.wasm); the
# guest sources, WIT, Cargo.toml, and build.sh are the tracked inputs.
# Patterns are unanchored so they match inside each nested guest crate.
target/
bindings.rs
Cargo.lock